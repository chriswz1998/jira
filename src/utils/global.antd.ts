import styled from '@emotion/styled'
import { Button, Input, InputNumber } from 'antd'

export const HeightBtn = styled(Button)`
  height: 4.8rem;
  width: 100%;
`

export const HeightInp = styled(Input)`
  height: 4.8rem;
`

export const HeightNumInp = styled(InputNumber)`
  height: 4.8rem;
  width: 32rem;
  line-height: 4.8rem;
`
